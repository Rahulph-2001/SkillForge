# SkillForge TURN Server Configuration
# This file is mounted into the coturn Docker container

# Listening on all interfaces
listening-port=3478
tls-listening-port=5349

# Use fingerprint in TURN messages
fingerprint

# Long-term credential mechanism
lt-cred-mech

# Static user credentials (set these to match your .env TURN_USERNAME and TURN_CREDENTIAL)
user=skillforge:SkillForge2026Turn!

# Realm - should match your domain
realm=skillforge.rahulph.cloud

# Relay port range
min-port=49152
max-port=65535

# Log to stdout for Docker
log-file=stdout

# Disable CLI
no-cli

# Verbose logging (disable in production for performance)
verbose
