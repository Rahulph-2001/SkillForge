"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CreateSubscriptionPlanUseCase = void 0;
const inversify_1 = require("inversify");
const types_1 = require("../../../infrastructure/di/types");
const SubscriptionPlan_1 = require("../../../domain/entities/SubscriptionPlan");
const AppError_1 = require("../../../domain/errors/AppError");
const UserRole_1 = require("../../../domain/enums/UserRole");
const messages_1 = require("../../../config/messages");
let CreateSubscriptionPlanUseCase = class CreateSubscriptionPlanUseCase {
    constructor(userRepository, subscriptionPlanRepository, featureRepository, subscriptionPlanMapper) {
        this.userRepository = userRepository;
        this.subscriptionPlanRepository = subscriptionPlanRepository;
        this.featureRepository = featureRepository;
        this.subscriptionPlanMapper = subscriptionPlanMapper;
    }
    async execute(adminUserId, dto) {
        // Verify admin privileges
        const adminUser = await this.userRepository.findById(adminUserId);
        if (!adminUser || adminUser.role !== UserRole_1.UserRole.ADMIN) {
            throw new AppError_1.ForbiddenError(messages_1.ERROR_MESSAGES.ADMIN.ACCESS_REQUIRED);
        }
        // Check if plan name already exists
        const existingPlan = await this.subscriptionPlanRepository.findByName(dto.name);
        if (existingPlan) {
            throw new AppError_1.ConflictError('A subscription plan with this name already exists');
        }
        // Auto-populate legacy fields if not provided but present in features
        let projectPosts = dto.projectPosts;
        let createCommunity = dto.createCommunity;
        if (projectPosts === undefined || projectPosts === null) {
            const feature = dto.features?.find(f => f.name.toLowerCase().includes('project') && f.name.toLowerCase().includes('post'));
            if (feature && feature.limitValue !== undefined) {
                projectPosts = feature.limitValue;
            }
        }
        if (createCommunity === undefined || createCommunity === null) {
            const feature = dto.features?.find(f => f.name.toLowerCase().includes('community') && f.name.toLowerCase().includes('create'));
            if (feature && feature.limitValue !== undefined) {
                createCommunity = feature.limitValue;
            }
        }
        // Create domain entity (features will be empty initially)
        const plan = new SubscriptionPlan_1.SubscriptionPlan('', // ID will be generated by database
        dto.name, dto.price, projectPosts ?? null, createCommunity ?? null, [], // Empty features array, features created separately
        dto.badge, dto.color, true, // isActive
        new Date(), new Date(), dto.trialDays ?? 0);
        // Save to repository (creates the plan)
        const createdPlan = await this.subscriptionPlanRepository.create(plan);
        // Create features for this plan
        if (dto.features && dto.features.length > 0) {
            // Import Feature and FeatureType dynamically or ensure they are imported at top
            // Assuming imports are present or will be added
            const { Feature } = await Promise.resolve().then(() => __importStar(require('../../../domain/entities/Feature')));
            const { v4: uuidv4 } = await Promise.resolve().then(() => __importStar(require('uuid')));
            const featurePromises = dto.features.map(async (featureDto) => {
                const feature = new Feature({
                    id: uuidv4(),
                    planId: createdPlan.id, // Link to the new plan
                    name: featureDto.name,
                    description: featureDto.description,
                    featureType: featureDto.featureType,
                    limitValue: featureDto.limitValue,
                    isEnabled: featureDto.isEnabled,
                    displayOrder: featureDto.displayOrder,
                    isHighlighted: featureDto.isHighlighted,
                    createdAt: new Date(),
                    updatedAt: new Date(),
                });
                return this.featureRepository.create(feature);
            });
            await Promise.all(featurePromises);
        }
        // Fetch the complete plan with features to return
        const completePlan = await this.subscriptionPlanRepository.findById(createdPlan.id);
        if (!completePlan) {
            throw new AppError_1.InternalServerError('Failed to retrieve created plan');
        }
        return this.subscriptionPlanMapper.toDTO(completePlan);
    }
};
exports.CreateSubscriptionPlanUseCase = CreateSubscriptionPlanUseCase;
exports.CreateSubscriptionPlanUseCase = CreateSubscriptionPlanUseCase = __decorate([
    (0, inversify_1.injectable)(),
    __param(0, (0, inversify_1.inject)(types_1.TYPES.IUserRepository)),
    __param(1, (0, inversify_1.inject)(types_1.TYPES.ISubscriptionPlanRepository)),
    __param(2, (0, inversify_1.inject)(types_1.TYPES.IFeatureRepository)),
    __param(3, (0, inversify_1.inject)(types_1.TYPES.ISubscriptionPlanMapper)),
    __metadata("design:paramtypes", [Object, Object, Object, Object])
], CreateSubscriptionPlanUseCase);
//# sourceMappingURL=CreateSubscriptionPlanUseCase.js.map