"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
var __param = (this && this.__param) || function (paramIndex, decorator) {
    return function (target, key) { decorator(target, key, paramIndex); }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CreateSubscriptionPlanUseCase = void 0;
const inversify_1 = require("inversify");
const types_1 = require("../../../infrastructure/di/types");
const SubscriptionPlan_1 = require("../../../domain/entities/SubscriptionPlan");
const AppError_1 = require("../../../domain/errors/AppError");
const UserRole_1 = require("../../../domain/enums/UserRole");
const messages_1 = require("../../../config/messages");
let CreateSubscriptionPlanUseCase = class CreateSubscriptionPlanUseCase {
    constructor(userRepository, subscriptionPlanRepository, subscriptionPlanMapper) {
        this.userRepository = userRepository;
        this.subscriptionPlanRepository = subscriptionPlanRepository;
        this.subscriptionPlanMapper = subscriptionPlanMapper;
    }
    async execute(adminUserId, dto) {
        // Verify admin privileges
        const adminUser = await this.userRepository.findById(adminUserId);
        if (!adminUser || adminUser.role !== UserRole_1.UserRole.ADMIN) {
            throw new AppError_1.ForbiddenError(messages_1.ERROR_MESSAGES.ADMIN.ACCESS_REQUIRED);
        }
        // Check if plan name already exists
        const existingPlan = await this.subscriptionPlanRepository.findByName(dto.name);
        if (existingPlan) {
            throw new AppError_1.ConflictError('A subscription plan with this name already exists');
        }
        // Generate feature IDs
        const features = dto.features.map((feature, index) => ({
            id: `${Date.now()}-${index}`,
            name: feature.name,
        }));
        // Create domain entity
        const plan = new SubscriptionPlan_1.SubscriptionPlan('', // ID will be generated by database
        dto.name, dto.price, dto.projectPosts, dto.communityPosts, features, dto.badge, dto.color, true, // isActive
        new Date(), new Date());
        // Save to repository
        const createdPlan = await this.subscriptionPlanRepository.create(plan);
        return this.subscriptionPlanMapper.toDTO(createdPlan);
    }
};
exports.CreateSubscriptionPlanUseCase = CreateSubscriptionPlanUseCase;
exports.CreateSubscriptionPlanUseCase = CreateSubscriptionPlanUseCase = __decorate([
    (0, inversify_1.injectable)(),
    __param(0, (0, inversify_1.inject)(types_1.TYPES.IUserRepository)),
    __param(1, (0, inversify_1.inject)(types_1.TYPES.ISubscriptionPlanRepository)),
    __param(2, (0, inversify_1.inject)(types_1.TYPES.ISubscriptionPlanMapper)),
    __metadata("design:paramtypes", [Object, Object, Object])
], CreateSubscriptionPlanUseCase);
//# sourceMappingURL=CreateSubscriptionPlanUseCase.js.map